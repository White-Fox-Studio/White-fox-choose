import { ServTranslatePipe } from './serv-translate.pipe';

describe('ServTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new ServTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
